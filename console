suportar selenium tambem (so um porem, no watir o browser eh um e driver eh outro (dentro da classe de element)) (talvez isso nao traga problema) e fazer spec pra ele

Fazer specs pras opções

checar methodo por metodo e todas as opçoes possiveis e arrumar opcoes no path no move_script

A fazer JS:
  
  Parar de loadar o YUI do servidor e já injetar o código dele

  Parar de poluir o global namespace

  colocar callback no js pra o selenium esperar executar o trem (pode usar a function de retorno)