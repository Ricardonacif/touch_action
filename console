fazer specs para:

pinch usando http://jsfiddle.net/gilbertolenzi/uZjCB/208/

Rotate usando:

No spec do pinch, por a propriedade do q ta mudando do style

Fazer spec pra ver se ta injetando a parada no element(shoudl respond_to)

refatorar pra tudo ser gerado de acordo com o nome e o numero de parametros 

suportar selenium tambem (so um porem, no watir o browser eh um e driver eh outro (dentro da classe de element)) (talvez isso nao traga problema)

Fazer specs pras opções

checar methodo por metodo e todas as opçoes possiveis e arrumar opcoes no path no move_script

A fazer JS:

colocar callback no js pra o selenium esperar executar o trem (pode usar a function de retorno)

refatorar todo o javascript